name: Docker Image CI

on:
  push:
    branches:
    - develop
    - release/*
    - feature/*

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag khaljimbo/safari-go:$(date +%s)
    - name: Push the Docker image
      run: docker push khaljimbo/safari-go:$(date +%s)
